dist/
node_modules/
pnpm-lock.yaml

# The specification documents are authored prose. Prettier reflows emphasis
# markers and creates diff noise in them for no benefit.
*.md

# The Python training pipeline and its data. The pilot JSON files are frozen
# evidence behind axes.md — reformatting them is pure diff noise on a record
# that is supposed to be stable.
model/
